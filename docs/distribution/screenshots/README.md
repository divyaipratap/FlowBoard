# Screenshot Assets

These SVGs are submission-ready placeholders that match the product story. Replace them with captured desktop screenshots after signed release builds are available.

Recommended final captures:

1. Settings -> Agent Bridge with MCP status ready and Copy config visible.
2. Issue detail with an attached WorkProof and green verified state.
3. Cursor next to FlowBoard showing ticket execution from MCP to completion.

Export final captures at:

```text
1600x1000 PNG
2880x1800 PNG for high-DPI directories
1200x630 PNG for social cards
```

